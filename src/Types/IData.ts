export interface IExperienceData {
  company: string;
  role: string;
  timeline: string;
  logo: string;
}

export interface IProjectsData {
  title: string;
  imageUrl: string;
  Description: string;
  tags: string[];
  sourceCode: string;
  liveLink: string;
}
